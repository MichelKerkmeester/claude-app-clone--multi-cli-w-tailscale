**/coverage/
**/dist/
**/node_modules/
release/evidence/
package-lock.json
# Runtime tooling symlinked into this app's root in the standalone deployment
.opencode/
.pi/
.claude/
.codex/
.cursor/
.devin/

# Spec packets are authored prose with their own template contracts; a formatter
# rewrapping them breaks anchor blocks and frontmatter the validator checks.
# specs/context/ is worse than that: six read-only research repositories vendored
# for reference. `prettier --write .` would have rewritten all of them.
specs/
screenshots/
