# Inbox overlay

`screen-attention-inbox.svelte` — the **attention inbox overlay**: the list of items needing the operator's attention. Like Review, it's an overlay shown by the shell (`routes/+layout.svelte`) above the routed page, not a URL of its own.

Single-component screen; state + actions come from shell context.
