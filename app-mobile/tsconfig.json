{
  "extends": ["../tsconfig.base.json", "./.svelte-kit/tsconfig.json"],
  "compilerOptions": {
    "allowJs": true,
    "checkJs": false
  },
  "//": "svelte-check discovers and type-checks every .svelte/.ts itself; this include is for tsc/editor scoping. allowJs keeps svelte.config.js (a .js file) in the program. The React→Svelte migration is complete — there are no .tsx files, so no jsx compiler options are needed.",
  "include": [
    ".svelte-kit/ambient.d.ts",
    ".svelte-kit/non-ambient.d.ts",
    ".svelte-kit/types/**/$types.d.ts",
    "vite.config.ts",
    "svelte.config.js",
    "src/app.d.ts",
    "src/routes/**/*.ts",
    "src/routes/**/*.svelte",
    "src/pages/**/*.ts",
    "src/pages/**/*.svelte",
    "src/shared/**/*.ts",
    "src/shared/**/*.svelte",
    ".storybook/**/*.ts"
  ],
  "exclude": ["node_modules", "src/service-worker.js"]
}
