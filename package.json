{
  "name": "pi-remote",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "src/*",
    "extensions/*"
  ],
  "scripts": {
    "build": "npm run build -w @pi-remote/pi-rpc-protocol && npm run build -w @pi-remote/relay && npm run build -w @pi-remote/web && npm run build -w @pi-remote/approval-extension && npm run build -w @pi-remote/plan-extension",
    "boot": "node scripts/boot.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "npm run typecheck -w @pi-remote/pi-rpc-protocol && npm run typecheck -w @pi-remote/relay && npm run typecheck -w @pi-remote/web && npm run typecheck -w @pi-remote/approval-extension && npm run typecheck -w @pi-remote/plan-extension",
    "test": "vitest run packages/pi-rpc-protocol/tests src/relay/tests extensions/pi-remote-approval/tests extensions/pi-remote-plan/tests tests",
    "test:web": "vitest run --config vitest.web.config.ts",
    "rollback:drill": "npm run build -w @pi-remote/relay && node scripts/rollback-drill.mjs",
    "verify:runtime-boundary": "npm run build -w @pi-remote/relay && node scripts/verify-full-access-runtime.mjs",
    "release:thresholds": "node scripts/check-thresholds.mjs",
    "release:rollout": "node scripts/check-rollout.mjs",
    "release:verify": "node scripts/release-verify.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.4",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@types/ws": "^8.18.1",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.4",
    "globals": "^17.11.0",
    "jsdom": "^29.1.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^3.2.4"
  }
}
